---
phase: 01-fix-composition-percentages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/controllers/composition/allocationAnalysis.js
  - backend/src/controllers/composition/sectorAnalysis.js
  - backend/src/controllers/composition/geographicAnalysis.js
  - backend/src/controllers/composition/holdingsAnalysis.js
  - backend/src/controllers/composition/bondRatingAnalysis.js
  - backend/src/controllers/composition/bondMaturityAnalysis.js
  - backend/src/controllers/composition/riskStatsAnalysis.js
autonomous: true

must_haves:
  truths:
    - "All 6 composition controllers use portfolio-weighted SUM(asset_weight * weight_percent) when portfolioId is present in query params"
    - "Backend returns correct weighted percentages that differ from simple AVG when assets have different portfolio values"
    - "Percentages in API responses sum to approximately 100% after normalization"
  artifacts:
    - path: "backend/src/controllers/composition/allocationAnalysis.js"
      provides: "Weighted allocation analysis for multiple assets"
      contains: "asset_weight.*weight_percent"
    - path: "backend/src/controllers/composition/sectorAnalysis.js"
      provides: "Weighted sector analysis for multiple assets"
      contains: "asset_weight.*weight_percent"
    - path: "backend/src/controllers/composition/geographicAnalysis.js"
      provides: "Weighted geographic analysis for multiple assets"
      contains: "asset_weight.*weight_percent"
    - path: "backend/src/controllers/composition/holdingsAnalysis.js"
      provides: "Weighted holdings analysis for multiple assets"
      contains: "asset_weight.*weight_percent"
    - path: "backend/src/controllers/composition/bondRatingAnalysis.js"
      provides: "Weighted bond rating analysis for multiple assets"
      contains: "asset_weight.*weight_percent"
    - path: "backend/src/controllers/composition/bondMaturityAnalysis.js"
      provides: "Weighted bond maturity analysis for multiple assets"
      contains: "asset_weight.*weight_percent"
  key_links:
    - from: "all 6 composition controllers"
      to: "v_current_positions"
      via: "SQL JOIN in if(portfolioId) branch"
      pattern: "v_current_positions.*WHERE portfolio_id"
    - from: "compositionAnalysisRoutes.js"
      to: "all 6 controllers"
      via: "Express route registration"
      pattern: "router\\.(get|post).*multiple"
---

<objective>
Verify and harden the 6 backend composition controllers' weighted calculation branches to ensure they produce correct portfolio-value-weighted percentages when portfolioId is provided.

Purpose: The backend already has the correct weighted SQL in each controller's `if (portfolioId)` branch. This plan verifies all 6 controllers follow the same pattern consistently, handle edge cases (no composition data, zero-value positions), and produce normalized results. This is the foundation the frontend fix relies on.

Output: All 6 composition controllers verified and hardened. riskStatsAnalysis.js also verified for portfolioId support.
</objective>

<execution_context>
@C:\Users\fpaol\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\fpaol\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-composition-percentages/01-CONTEXT.md
@.planning/phases/01-fix-composition-percentages/01-RESEARCH.md

# Key source files
@backend/src/controllers/composition/allocationAnalysis.js
@backend/src/controllers/composition/sectorAnalysis.js
@backend/src/controllers/composition/geographicAnalysis.js
@backend/src/controllers/composition/holdingsAnalysis.js
@backend/src/controllers/composition/bondRatingAnalysis.js
@backend/src/controllers/composition/bondMaturityAnalysis.js
@backend/src/controllers/composition/riskStatsAnalysis.js
@backend/src/routes/compositionAnalysisRoutes.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 6 composition controllers for consistent weighted query pattern</name>
  <files>
    backend/src/controllers/composition/allocationAnalysis.js
    backend/src/controllers/composition/sectorAnalysis.js
    backend/src/controllers/composition/geographicAnalysis.js
    backend/src/controllers/composition/holdingsAnalysis.js
    backend/src/controllers/composition/bondRatingAnalysis.js
    backend/src/controllers/composition/bondMaturityAnalysis.js
    backend/src/controllers/composition/riskStatsAnalysis.js
  </files>
  <action>
    Read each controller's `getXxxByMultipleAssets()` function and verify:

    1. Each function extracts `portfolioId` from `req.query` (pattern: `const { assetIds, portfolioId, ... } = req.query;`)
    2. Each has `if (portfolioId)` branch with weighted SQL using:
       - CTE `asset_weights AS (SELECT asset_id, current_value / SUM(current_value) OVER() as asset_weight FROM v_current_positions WHERE portfolio_id = $2 AND asset_id = ANY($1))`
       - Main query using `SUM(aw.asset_weight * a.weight_percent) as real_weight`
    3. Each has `else` fallback branch with `AVG()` (keep as safety fallback)
    4. Normalization logic exists (the "Altri" residual calculation pattern at the end)

    Also check `riskStatsAnalysis.js` for `getMultipleAssetsRiskStats()` — verify it also accepts and uses `portfolioId` for weighted stats.

    For each controller, document in a brief comment at the top of the function if the weighted branch is present and correct. Use the pattern:
    ```
    // Weighted calculation: verified 2026-02-05 — uses v_current_positions for portfolio weights
    ```

    If ANY controller is missing the weighted branch or has a different SQL pattern, fix it to match the allocationAnalysis.js reference pattern (lines 206-311 as documented in RESEARCH.md).

    Do NOT modify the else/fallback branches — they serve as safety fallbacks.
    Do NOT modify the normalization logic unless it's clearly broken.
    Do NOT add new parameters or change the function signatures.
  </action>
  <verify>
    For each of the 7 files (6 composition + riskStats):
    - Grep for `portfolioId` in the `ByMultipleAssets` function — must be present
    - Grep for `v_current_positions` — must appear in the if(portfolioId) branch
    - Grep for `asset_weight.*weight_percent` or equivalent weighted calculation
    - Run `node -c backend/src/controllers/composition/allocationAnalysis.js` (syntax check) for each file
    - Start backend server briefly to confirm no import/require errors: `cd backend && node -e "require('./src/controllers/composition/allocationAnalysis'); console.log('OK')"`
  </verify>
  <done>
    All 6 composition controllers + riskStatsAnalysis.js have verified weighted query branches that use v_current_positions for portfolio-value-weighted calculations when portfolioId is provided. Each function has a verification comment. No syntax errors. Backend starts without import errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify route wiring passes portfolioId to controllers</name>
  <files>
    backend/src/routes/compositionAnalysisRoutes.js
  </files>
  <action>
    Read `compositionAnalysisRoutes.js` and verify that:

    1. All 6 `/assets/multiple` routes are registered and point to the correct controller functions:
       - GET `/holdings/assets/multiple` -> holdingsAnalysis.getHoldingsByMultipleAssets
       - GET `/sectors/assets/multiple` -> sectorAnalysis.getSectorsByMultipleAssets
       - GET `/geographic/assets/multiple` -> geographicAnalysis.getGeographicByMultipleAssets
       - GET `/allocation/assets/multiple` -> allocationAnalysis.getAllocationByMultipleAssets
       - GET `/bond-ratings/assets/multiple` -> bondRatingAnalysis.getBondRatingsByMultipleAssets
       - GET `/bond-maturity/assets/multiple` -> bondMaturityAnalysis.getBondMaturityByMultipleAssets
    2. The risk-stats route is also registered:
       - GET `/risk-stats/assets/multiple` -> riskStatsAnalysis.getMultipleAssetsRiskStats
    3. Routes use GET method (query params are passed via URL, so portfolioId comes through req.query automatically — no middleware needed)

    If any route is missing, add it following the existing pattern.
    If routes use POST instead of GET, that's fine — just verify the parameter extraction matches (req.body vs req.query).

    No changes expected here — this is verification only. Only modify if a route is genuinely missing or misconfigured.
  </action>
  <verify>
    - Grep for all 7 route patterns in compositionAnalysisRoutes.js
    - Each route must map to the correct controller function
    - Run `node -c backend/src/routes/compositionAnalysisRoutes.js` for syntax check
  </verify>
  <done>
    All 7 composition analysis routes (6 composition types + risk-stats) are verified to be correctly registered, mapping to the right controller functions. Query parameters (including portfolioId) flow through to controllers via Express req.query automatically.
  </done>
</task>

</tasks>

<verification>
Backend verification checklist:
1. All 6 composition controllers have `if (portfolioId)` weighted branch using v_current_positions
2. riskStatsAnalysis.js supports portfolioId parameter
3. All routes correctly registered in compositionAnalysisRoutes.js
4. No syntax errors in any modified file
5. Backend can start without import errors
</verification>

<success_criteria>
- All 7 controller functions verified to have portfolio-weighted calculation branches
- Route wiring confirmed for all 7 endpoints
- No regressions introduced — existing functionality preserved
- Backend starts cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-composition-percentages/01-01-SUMMARY.md`
</output>
